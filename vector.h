#ifndef VECTORH
#define VECTORH

#include <math.h>

#include "datatypes.h"
#include "types.h"

struct point3D normalize(struct point3D p) ;
struct point3D vector(struct point3D u, struct point3D ) ;
float dot(struct point3D u, struct point3D v) ;
float length(struct point3D u) ;

#endif
